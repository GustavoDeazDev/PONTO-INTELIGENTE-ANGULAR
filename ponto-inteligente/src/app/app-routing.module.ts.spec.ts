import { AppRouting.Module.Ts } from './app-routing.module.ts';

describe('AppRouting.Module.Ts', () => {
  it('should create an instance', () => {
    expect(new AppRouting.Module.Ts()).toBeTruthy();
  });
});
